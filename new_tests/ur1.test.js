import request from "supertest";
import { app } from "../app.js";

describe("UR1: Users should be able to authenticate with their Microsoft account", () => {
    test("FR1: The system should allow users to register an account via Microsoft credentials", async () => {});
});